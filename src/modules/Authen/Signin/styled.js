import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
    alignItems: 'center',
    marginTop: 60,
    marginHorizontal: 30
  },
  logoImg: {
    width: 82,
    height: 82
  }
})
